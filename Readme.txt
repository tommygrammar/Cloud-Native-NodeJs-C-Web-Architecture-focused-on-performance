the rapper.c is a shared memory program,

compile it using : gcc -o wrapper wrapper.c -Ofast

refer to my super cache for full setup

after full compilation, simply run set.js: node set.js

have fun

this servers data directly to frontend without using any apis, instead, it relies on a C program that accesses memory

built for high performance